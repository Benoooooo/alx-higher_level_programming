#!/usr/bin/node
const args = process.argv.slice(2);
const first = args[0];
const sec = args[1];

function add(a, b) {
  if (Number.isInteger(parseInt(first),(sec))) {
    let sum = first + sec;
    console.log(sum);
  }
}
add(a 
